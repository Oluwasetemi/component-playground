run:
    cargo run --release -p gpui-starter-desktop

build:
    cargo build --release -p gpui-starter-desktop

check:
    cargo check -p gpui-starter-ui -p gpui-starter-desktop

dev:
    cargo run -p gpui-starter-desktop
